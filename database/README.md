# Database

PostgreSQL and Prisma schema for RakthaSethu.

## Prisma schema
- Path: `database/prisma/schema.prisma`
- Shared by backend service.

## Local PostgreSQL
```bash
docker compose up -d
```

Default local credentials are for development only:
- user: `postgres`
- password: `postgres`
- database: `rakthasethu`
