{
  "name": "rakthasethu",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "dev:api": "npm run dev --workspace @rakthasethu/api",
    "build:web": "npm run build --workspace web",
    "build:api": "npm run build --workspace @rakthasethu/api",
    "test:api": "npm run test --workspace @rakthasethu/api"
  }
}
